# Copy this file to .env.local and fill in the value.
# Required for local development if you want to point at the deployed Render backend.
# Leave empty to use the Vite dev proxy (localhost:8000).
VITE_API_BASE_URL=https://your-render-app.onrender.com
